# MIDL MCP Server Configuration

# Required: Private key for signing transactions (hex-encoded, with or without 0x prefix)
MIDL_PRIVATE_KEY=

# Network selection: "mainnet" or "regtest" (default: regtest)
MIDL_NETWORK=regtest

# Transport mode: "stdio" or "http" (default: stdio)
MCP_TRANSPORT=stdio

# HTTP port (only used when MCP_TRANSPORT=http)
PORT=3001

# Log level: "debug", "info", "warn", "error" (default: info)
LOG_LEVEL=info
